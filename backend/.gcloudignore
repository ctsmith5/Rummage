# Git (exclude git history, but files are tracked)
.git/
.gitignore

# IDE files
.idea/
.vscode/
*.swp
*.swo
*.sublime-*

# Local build artifacts (Windows)
*.exe
*.dll
server.exe
server

# Environment files
.env
.env.local
*.env

# Local uploads directory (not needed in container)
uploads/

# Keep all source code - don't exclude cmd/, internal/, etc.